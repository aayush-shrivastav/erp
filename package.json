{
  "name": "college-erp",
  "version": "1.0.0",
  "description": "College ERP System - Full Stack Application",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "seed:admin": "cd backend && node seedAdmin.js",
    "seed:faculty": "cd backend && node seedFaculty.js",
    "seed:students": "cd backend && node seedStudentData.js"
  },
  "keywords": [
    "erp",
    "college",
    "education",
    "mongodb",
    "express",
    "react"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
